<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Access Denied — Aurora Studio</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<div class="digits" id="digits"></div>

<div class="card hero-card" style="max-width:600px; text-align:center;">
  <h1 class="hero-title">Access Denied</h1>

  <p class="hero-desc">
    Aurora Studio requires names with at least 4 characters
    to personalize your cinematic experience.
  </p>

  <a href="/" class="btn">Try Again</a>
</div>

<script>
const cont = document.getElementById("digits");
const chars="0123456789";
for(let i=0;i<12;i++){
  const d=document.createElement("div");
  d.className="digit";
  d.textContent=chars[Math.floor(Math.random()*chars.length)];
  d.style.left=Math.random()*100+"vw";
  d.style.fontSize=(12+Math.random()*34)+"px";
  d.style.animationDelay=(Math.random()*6)+"s";
  cont.appendChild(d);
}
</script>

</body>
</html>
