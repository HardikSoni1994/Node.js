<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Enter Name — Aurora Studio</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<div class="digits" id="digits"></div>

<div class="container">
  <div class="left">
    <div class="logo">AURORA • STUDIO</div>

    <h1>Quick check — tell us your name</h1>

    <p class="lead">
      Enter a name of at least 4 characters. Aurora Studio personalizes your cinematic
      demo experience — your name will appear on the welcome screen.
    </p>
  </div>

  <div class="card hero-card">
    <h2 class="hero-title">Verify Name</h2>
    <p class="hero-desc">Enter your name to continue into Aurora Studio.</p>

    <form action="/" method="get">
      <div class="input">
        <input type="text" name="name" placeholder="e.g. Nova">
      </div>

      <button class="btn">Continue →</button>
    </form>

    <p style="color:var(--muted); font-size:13px; margin-top:10px">
      Rule: <code>name.length &gt; 3</code>
    </p>
  </div>
</div>

<script>
const cont = document.getElementById("digits");
const chars = "0123456789";
for(let i=0;i<14;i++){
  const d=document.createElement("div");
  d.className="digit";
  d.textContent = chars[Math.floor(Math.random()*chars.length)];
  d.style.left = Math.random()*100 + "vw";
  d.style.fontSize = (12+Math.random()*34)+"px";
  d.style.animationDelay = (Math.random()*6)+"s";
  cont.appendChild(d);
}
</script>

</body>
</html>
